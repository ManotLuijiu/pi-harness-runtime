# AI Brief — RFC-0042 Context Compiler

Build a bounded context selection engine that combines project rules, OKF knowledge, repository slices, blackboard state, test failures, and prior reports.

Never dump the entire repository. Every selected item needs a source reference; every omitted item needs a reason.
